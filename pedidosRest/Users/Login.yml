info:
  name: Login
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/Login?username=admin1&password=admin
  params:
    - name: username
      value: admin1
      type: query
    - name: password
      value: admin
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
