info:
  name: Login
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/Login?username=admin&password=admin1
  params:
    - name: username
      value: admin
      type: query
    - name: password
      value: admin1
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
