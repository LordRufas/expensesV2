info:
  name: CreateUser
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/createUser?username=admin1&password=admin12
  params:
    - name: username
      value: admin1
      type: query
    - name: password
      value: admin12
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
