info:
  name: CoreUpdate
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8080/update
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
