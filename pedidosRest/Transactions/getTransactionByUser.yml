info:
  name: getTransactionByUser
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/getTransactionByUser?id=0
  params:
    - name: id
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
