info:
  name: CoreInsert
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/insert
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
