info:
  name: CoreDelete
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:8080/delete
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
